import styled from 'styled-components';

export const Container = styled.div`
    background: rgba(0, 0, 0, 0.7);
    display: flex;
    align-items: center;
    margin-bottom: 30px;
    padding: 8px;
    position: relative;

    img {
        width: 48px;
        height: 48px;
        margin-left: 30px;
    }
`;
